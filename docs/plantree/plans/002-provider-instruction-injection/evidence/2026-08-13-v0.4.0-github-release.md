# v0.4.0 GitHub Release Evidence

Date: 2026-08-13
Role: evidence
Status: accepted
Related: [plan](../README.md), [roadmap](../roadmap.md), [decision 002](../decisions/002-github-release-before-registries.md), [release notes](../../../../releases/v0.4.0.md)

## Published Source Release

- Commit: `763e4a05bdb2a3b3fbcb13085f6edefc2b0bad5f`.
- Annotated tag: `v0.4.0`; the remote tag object dereferences to that exact commit.
- Formal GitHub Release: [v0.4.0](https://github.com/SeemSeam/plan-tree/releases/tag/v0.4.0), published at `2026-08-13T04:41:34Z`; it is neither a draft nor a prerelease.
- The published body contains both exact section headings, `## English` and `## 中文`.
- Release workflow: [run 31667800673](https://github.com/SeemSeam/plan-tree/actions/runs/31667800673), triggered by the tag at the same commit.

## Workflow Results

| Job | Result | Evidence |
| --- | --- | --- |
| Validate release source | success | Tag, bilingual notes, twenty repository tests, syntax, and CLI versions passed. |
| Create GitHub Release | success | The formal bilingual Release was created. |
| Publish to npm | failure | Package inspection passed; `npm publish --access public` returned `E404 Not Found` or insufficient permission. |
| Publish to PyPI | skipped | The job correctly remained behind the failed npm dependency. |

## Registry State

- npm still exposes `plan-tree@0.2.2`; `plan-tree@0.4.0` returns `E404`.
- PyPI still returns `404` for `seemseam-plan-tree/0.4.0`.
- The npm package has an existing owner, so an owner must confirm or correct the Trusted Publisher/package authorization for repository `SeemSeam/plan-tree`, workflow `release.yml`, and the intended publish action.

## Recovery Contract

Keep the existing `v0.4.0` tag immutable. After npm authorization is corrected, rerun the release workflow or registry path against that tag. The GitHub Release job is idempotent and verifies the existing bilingual body; npm must succeed before PyPI runs. Treat the release as fully complete only after both registries expose `0.4.0` and clean installs pass.
