# v0.4.0 Release Candidate Evidence

Date: 2026-08-13
Role: evidence
Status: accepted
Related: [plan](../README.md), [roadmap](../roadmap.md), [provider instruction contract](../topics/provider-instruction-contract.md), [release notes](../../../../releases/v0.4.0.md)

## Claim Verified

The `0.4.0` source candidate implements equivalent safe persistent-instruction injection for the Python and npm installers and is ready to commit. Package publication remains blocked until npm Trusted Publisher configuration is confirmed.

## Source Verification

- `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v`: 19 tests passed.
- `node --check bin/plan-tree.js`: passed.
- `git diff --check`: passed.
- Version `0.4.0` agrees across `VERSION`, `pyproject.toml`, `package.json`, both CLI constants, and the Python package `__version__`.
- Local Markdown links, Plan IDs and statuses, module keys, task authority, public English/Chinese contracts, and release-note paths pass repository checks.

## Package Verification

- `python -m build` produced a six-entry wheel and an eight-entry sdist.
- `python -m twine check` accepted both Python artifacts.
- `npm pack` produced a five-entry tarball containing only `README.md`, `README.zh-CN.md`, `VERSION`, `bin/plan-tree.js`, and `package.json`.
- Fresh wheel and npm-tarball installs both reported `0.4.0`.
- Each installed CLI injected exactly one managed block for Claude Code, OpenCode, and Codex in an isolated home.
- [Bilingual release notes](../../../../releases/v0.4.0.md) passed the release-publisher semantic-structure validator.

## Candidate Checksums

| Artifact | SHA-256 |
| --- | --- |
| `seemseam_plan_tree-0.4.0-py3-none-any.whl` | `3d45a5bd278254150f10624d468921cdf9163d9de7be248acc0bdcfce1bacdf4` |
| `seemseam_plan_tree-0.4.0.tar.gz` | `f598fd2773db5a04a045d3e020357ac202c9be3ad308cff57df70dd30310d637` |
| `plan-tree-0.4.0.tgz` | `b107b0bf5c575c73968356129c60605336a8a0d3d006555c3703924e985c592b` |

These are local release-candidate artifacts. The tag workflow rebuilds registry artifacts from the committed tag and must be verified independently after publication.

## Registry And Workflow Gate

- `v0.4.0` was absent from local and remote tags, GitHub Releases, PyPI, and npm during the release audit.
- PyPI currently exposes `0.3.0`; npm currently exposes `0.2.2`.
- The workflow validates source first, publishes npm through OIDC, publishes PyPI through OIDC, then creates the bilingual GitHub Release.
- The npm job uses npm `11.15.0`, has job-scoped `id-token: write`, and does not expose `NPM_TOKEN` or `NODE_AUTH_TOKEN`.
- A read-only `npm trust list plan-tree --json` attempt returned `401 Unauthorized` in the current environment.

Required npm trust values before tagging:

| Field | Value |
| --- | --- |
| Package | `plan-tree` |
| Provider | GitHub Actions |
| Organization or user | `SeemSeam` |
| Repository | `plan-tree` |
| Workflow filename | `release.yml` |
| Environment | blank |
| Allowed action | `npm publish` |

## External Actions

No commit, push, tag, GitHub Release, PyPI upload, or npm publication had occurred when this evidence was recorded.

## Subsequent Source Landing

The reviewed candidate was committed as `dd9031fbc645ac87fcdb9edf0d7d33a3808b0932` and pushed to `origin/main`. GitHub accepted that commit and recognizes `.github/workflows/release.yml`. No release tag or registry publication occurred; npm trust remains the release gate.
